#ifndef one
#define one

#include <cstdlib>
#include <cstring>
#include <ctime>
#include <iostream>

using namespace std;

int game(int Num, int Count, bool Correct);
int CheckNik(string p);
int menu(int r);

#endif